import subprocess
import time

def run_docker_compose():
    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose1.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose2.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose3.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose4.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose5.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose6.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose7.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose8.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose9.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose10.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose11.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose12.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose13.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose14.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose15.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose16.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose17.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose18.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose19.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose20.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose21.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose22.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose23.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")

    time.sleep(10)

    try:
        # Define the command
        command = ["docker-compose", "-f", "docker-compose24.yml", "up", "-d"]

        # Run the command
        subprocess.run(command, check=True)
        print("Docker Compose command executed successfully.")
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")
    ###########################################################################################################
    
    
    # Kurze Wartezeit (10 Sekunden), um sicherzustellen, dass die Container gestartet wurden
    time.sleep(10)
    
    # Lösche alle temporären Volume Dateien
    try:
        subprocess.run(['docker', 'system', 'prune', '-a', '--volumes'], check=True, input='y', text=True)
        print("Cleanup successful.")
    except subprocess.CalledProcessError as e:
        print(f"Error cleaning up: {e}")